/**
 * `@pi-cmux/policy` — fail-closed admission control.
 *
 * Default deny, one rule at a time, every decision auditable as an event.
 */

export * from "./path-containment.ts";
export * from "./decide.ts";
