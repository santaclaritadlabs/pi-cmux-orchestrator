{
  "name": "@pi-cmux/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lifecycle state machine and durable run store.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --test --test-reporter=spec --enable-source-maps \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@pi-cmux/protocol": "workspace:*",
    "@pi-cmux/observability": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@pi-cmux/testkit": "workspace:*"
  }
}
