{
  "name": "@pi-cmux/cmux",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Optional bridge from agentd status projections to the external cmux CLI/socket API.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --test --test-reporter=spec --enable-source-maps \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@pi-cmux/pi-extension": "workspace:*",
    "@pi-cmux/protocol": "workspace:*"
  }
}
