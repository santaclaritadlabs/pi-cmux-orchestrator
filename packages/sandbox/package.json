{
  "name": "@pi-cmux/sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sandbox abstraction, capability discovery and fail-closed isolation selection.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --test --test-reporter=spec --enable-source-maps \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@pi-cmux/observability": "workspace:*",
    "@pi-cmux/policy": "workspace:*",
    "@pi-cmux/process-supervisor": "workspace:*",
    "@pi-cmux/protocol": "workspace:*"
  },
  "devDependencies": {
    "@pi-cmux/testkit": "workspace:*"
  }
}
