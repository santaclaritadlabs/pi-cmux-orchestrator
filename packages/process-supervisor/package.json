{
  "name": "@pi-cmux/process-supervisor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded worker process supervision: spawn, cancel, timeouts, output limits.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --test --test-reporter=spec --enable-source-maps \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@pi-cmux/policy": "workspace:*",
    "@pi-cmux/protocol": "workspace:*",
    "@pi-cmux/observability": "workspace:*",
    "@pi-cmux/testkit": "workspace:*"
  }
}
